name: Deploy Backend

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Deployment environment"
        required: true
        type: choice
        options:
          - dev
          - prod

      template:
        description: "Template name, e.g. dev-mintest"
        required: true
        type: string
        
      operation:
        description: "Terraform action to perform, e.g. apply or destroy"
        required: true
        type: choice
        options:
          - apply
          - destroy
      
      workspace:
        description: "Terraform workspace, e.g. backend-dev-01"
        required: true
        type: string

jobs:
  trigger-airflow-ami-build:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Backend Terraform deployment
        env:
          GH_TOKEN: ${{ secrets.CICD_TRIGGER_TOKEN }}
        run: |
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "X-GitHub-Api-Version: 2026-03-10" \
            https://api.github.com/repos/Housing-Intelligence/hi-platform-cicd/actions/workflows/terraform.yml/dispatches \
            -d '{
              "ref": "main",
              "inputs": {
                "target_repository": "Housing-Intelligence/hi-app-terraforms",
                "terraform_directory": "airflow/terraform",
                "operation": "${{ inputs.operation }}",
                "parameters_json": "{\"workspace\":\"airflow-${{ inputs.workspace }}\"}"
              }
            }'